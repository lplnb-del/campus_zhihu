<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.zhihu.mapper.TagMapper">

    <!-- 批量更新标签使用次数 -->
    <update id="batchUpdateUseCount">
        UPDATE biz_tag
        SET use_count = use_count + #{delta}
        WHERE id IN
        <foreach collection="tagIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <!-- 根据ID列表查询标签 -->
    <select id="selectByIds" resultType="com.campus.zhihu.entity.BizTag">
        SELECT *
        FROM biz_tag
        <where>
            <if test="tagIds != null and tagIds.size() > 0">
                id IN
                <foreach collection="tagIds" item="id" open="(" separator="," close=")">
                    #{id}
                </foreach>
            </if>
            AND is_deleted = 0
        </where>
    </select>

</mapper>