# CampusZhihu 代码审查综合报告

> **审查日期**：2026-01-11
> **审查范围**：后端Java代码、前端Vue/TypeScript代码、前后端接口一致性
> **审查方法**：静态代码分析 + 人工审查

---

## 📊 总体评分

| 维度 | 后端 | 前端 | 接口一致性 | 综合评分 |
|------|------|------|------------|----------|
| **代码正确性** | 8/10 | 7/10 | 6/10 | **7.0/10** |
| **代码一致性** | 7/10 | 7/10 | 6/10 | **6.7/10** |
| **功能完成度** | 9/10 | 7/10 | 7/10 | **7.7/10** |
| **代码优美度** | 8/10 | 7/10 | - | **7.5/10** |
| **综合评分** | **7.5/10** | **7.0/10** | **6.0/10** | **7.0/10** |

---

## 🚨 P0级别问题（必须立即修复）

### 后端问题

| # | 文件 | 问题描述 | 修复方案 |
|---|------|----------|----------|
| 1 | `QuestionController.java:47` | **格式错误**：代码缩进混乱，可读性极差 | 修正代码格式 |
| 2 | `ResultCode.java:93` | **错误码重复**：`INSUFFICIENT_POINTS` 使用了已占用的错误码2005 | 修改为2006 |
| 3 | `SecurityConfig.java:70` | **权限过宽**：`/user/**` 完全开放，包括敏感接口 | 只开放查询接口 |
| 4 | `application.yml:46` | **安全隐患**：Druid监控密码为空 | 设置默认密码 |

### 前端问题

| # | 文件 | 问题描述 | 修复方案 |
|---|------|----------|----------|
| 1 | `api/collection.ts` | **文件缺失**：收藏API模块不存在 | 创建该文件 |
| 2 | `views/user/Settings.vue` | **组件缺失**：用户设置页面不存在 | 创建该组件 |
| 3 | `types/` 目录 | **类型管理缺失**：没有统一的类型定义目录 | 创建types/index.ts |

### 接口一致性问题

| # | 接口 | 问题描述 | 修复方案 |
|---|------|----------|----------|
| 1 | `GET /user/{userId}/stats` | **接口缺失**：前端调用但后端未实现 | 在UserController添加 |
| 2 | `GET /tag/{tagId}/followed` | **接口缺失**：前端调用但后端未实现 | 在TagController添加 |
| 3 | `GET /tag/{tagId}/question-count` | **接口缺失**：前端调用但后端未实现 | 在TagController添加 |
| 4 | `POST /question/search` | **参数不一致**：前端用`page/size`，后端用`pageNum/pageSize` | 统一参数命名 |

---

## ⚠️ P1级别问题（重要，应尽快修复）

### 后端问题

| # | 文件 | 问题描述 | 修复方案 |
|---|------|----------|----------|
| 1 | `UserController.java:131` | **空指针风险**：`getCurrentUserId` 在info接口可能抛异常 | 改用`getCurrentUserIdOrNull` |
| 2 | `TagController.java:176` | **业务逻辑位置错误**：关注标签逻辑在Controller而非Service | 移至TagService |
| 3 | `CommentController.java:333` | **权限验证缺失**：管理员功能未验证角色 | 添加`@PreAuthorize` |
| 4 | `LikeController.java:145` | **返回实体类风险**：直接返回`BizLikeRecord` | 使用VO封装 |

### 前端问题

| # | 问题描述 | 修复方案 |
|---|------|----------|
| 1 | **类型定义不统一**：CollectionItem等接口在各组件中重复定义 | 创建types/index.ts统一管理 |
| 2 | **环境变量使用不一致**：生产环境硬编码API地址 | 使用VITE_API_BASE_URL |
| 3 | **工具函数重复**：formatNumber等函数在多个组件中重复 | 提取到utils/format.ts |
| 4 | **路由守卫缺失**：没有登录验证和权限控制 | 添加router.beforeEach |

### 接口一致性问题

| # | 接口 | 问题描述 | 修复方案 |
|---|------|----------|----------|
| 1 | `GET /question/user/{userId}` | **缺少分页**：前端期望分页，后端返回List | 添加分页参数支持 |
| 2 | `GET /answer/user/{userId}` | **缺少分页**：前端期望分页，后端返回List | 添加分页参数支持 |
| 3 | `GET /comment/my` | **缺少分页**：前端期望分页，后端返回List | 添加分页参数支持 |
| 4 | `GET /like/my` | **缺少分页**：前端期望分页，后端返回List | 添加分页参数支持 |
| 5 | `POST /like/check-batch` | **参数格式不一致**：参数传递方式不统一 | 统一为请求体或查询参数 |

---

## 💡 P2级别问题（优化建议）

### 后端优化

| # | 建议 | 预期收益 |
|---|------|----------|
| 1 | 统一分页接口返回格式（IPage vs PageResponse） | 提高代码一致性 |
| 2 | 删除TagController中重复的getUserIdFromRequest方法 | 减少代码重复 |
| 3 | 将QuestionController.getLong方法抽取为工具类 | 提高代码复用性 |
| 4 | 使用AOP统一处理分页参数验证 | 减少重复代码 |

### 前端优化

| # | 建议 | 预期收益 |
|---|------|----------|
| 1 | 创建composables/usePagination统一分页逻辑 | 减少重复代码 |
| 2 | 创建composables/useLoading统一加载状态 | 提高代码复用性 |
| 3 | 提取公共样式到global.scss | 减少样式重复 |
| 4 | 使用dayjs统一日期处理 | 提高一致性 |

---

## 📈 代码完成度检查

### ✅ 已完成

| 模块 | 完成度 | 说明 |
|------|--------|------|
| 用户认证模块 | 100% | 注册、登录、双Token认证完整 |
| 问题管理模块 | 95% | 核心功能完整，stats接口缺失 |
| 回答管理模块 | 90% | 核心功能完整，分页需完善 |
| 评论互动模块 | 85% | 二级评论功能完整，分页需完善 |
| 标签管理模块 | 85% | 核心功能完整，部分接口缺失 |
| 点赞收藏模块 | 90% | 核心功能完整，分页需完善 |

### ⚠️ 需补充

| 功能 | 位置 | 状态 |
|------|------|------|
| 用户统计接口 | UserController | ❌ 缺失 |
| 标签关注检查 | TagController | ❌ 缺失 |
| 标签问题数量 | TagController | ❌ 缺失 |
| 用户设置页面 | views/user/ | ❌ 缺失 |
| 收藏API模块 | api/ | ❌ 缺失 |
| 路由守卫 | router/index.ts | ❌ 缺失 |

---

## 🎯 快速修复指南

### 第一步：修复格式和错误码问题（5分钟）

```bash
# 1. 修复QuestionController格式问题
# 文件：backend/.../QuestionController.java
# 行数：47
# 修正方法：重新格式化代码

# 2. 修复错误码重复
# 文件：backend/.../ResultCode.java
# 行数：93
# 修改：INSUFFICIENT_POINTS(2006, "积分不足")
```

### 第二步：添加缺失的接口（30分钟）

```java
// 1. 添加用户统计接口
// 文件：backend/.../UserController.java
@GetMapping("/{userId}/stats")
public Result<Map<String, Integer>> getUserStats(@PathVariable Long userId) {
    Map<String, Integer> stats = new HashMap<>();
    stats.put("questionCount", questionService.countByUserId(userId));
    stats.put("answerCount", answerService.countByUserId(userId));
    stats.put("collectionCount", collectionService.countByUserId(userId));
    stats.put("likeCount", likeService.countByUserId(userId));
    stats.put("points", userService.getById(userId).getPoints());
    return Result.success(stats);
}

// 2. 添加标签关注检查接口
// 文件：backend/.../TagController.java
@GetMapping("/{tagId}/followed")
public Result<Boolean> checkFollowed(@PathVariable Long tagId) {
    Long userId = getCurrentUserId();
    boolean followed = tagService.isFollowed(userId, tagId);
    return Result.success(followed);
}

// 3. 添加标签问题数量接口
// 文件：backend/.../TagController.java
@GetMapping("/{tagId}/question-count")
public Result<Integer> getQuestionCount(@PathVariable Long tagId) {
    int count = questionService.countByTagId(tagId);
    return Result.success(count);
}
```

### 第三步：创建缺失的前端文件（20分钟）

```typescript
// 1. 创建收藏API模块
// 文件：frontend/src/api/collection.ts
import { get, del, post } from '@/utils/request'

export interface CollectionVO {
  id: number
  targetType: number
  targetId: number
  createTime: string
  question?: {
    id: number
    title: string
  }
  answer?: {
    id: number
    content: string
  }
}

export const getMyCollections = (page: number, size: number) => {
  return get<{
    list: CollectionVO[]
    total: number
  }>('/collection/my', { params: { page, size } })
}

export const removeCollection = (targetType: number, targetId: number) => {
  return del<void>('/collection/remove', { params: { targetType, targetId } })
}

export const checkCollectedBatch = (targetType: number, targetIds: number[]) => {
  return post<Record<number, boolean>>('/collection/check-batch', {
    targetType,
    targetIds,
  })
}
```

```typescript
// 2. 创建工具函数
// 文件：frontend/src/utils/format.ts
export const formatNumber = (num: number | undefined | null): string => {
  if (num === undefined || num === null) return '0'
  if (num >= 10000) return (num / 10000).toFixed(1) + 'w'
  if (num >= 1000) return (num / 1000).toFixed(1) + 'k'
  return num.toString()
}

// 3. 创建统一类型定义
// 文件：frontend/src/types/index.ts
export interface CollectionItem {
  id: number
  targetType: number
  targetId: number
  createTime: string
  question?: any
  answer?: any
}

export interface QuestionVO {
  id: number
  title: string
  content: string
  // ...
}
```

```typescript
// 4. 添加路由守卫
// 文件：frontend/src/router/index.ts
router.beforeEach((to, from, next) => {
  const userStore = useUserStore()
  const requiresAuth = to.meta.requiresAuth

  if (requiresAuth && !userStore.isLoggedIn) {
    next({
      path: '/login',
      query: { redirect: to.fullPath }
    })
  } else {
    next()
  }
})
```

### 第四步：统一参数命名（10分钟）

```typescript
// 前端搜索接口修改
// 文件：frontend/src/api/question.ts
export function searchQuestions(keyword: string, pageNum = 1, pageSize = 20) {
  return get<PageResponse<QuestionVO>>("/question/search", {
    keyword,
    pageNum,  // 改为pageNum
    pageSize, // 改为pageSize
  })
}
```

---

## 📊 问题统计

### 按严重程度统计

| 级别 | 后端 | 前端 | 接口 | 合计 |
|------|------|------|------|------|
| P0 | 4 | 3 | 4 | **11** |
| P1 | 4 | 4 | 5 | **13** |
| P2 | 4 | 4 | - | **8** |
| **合计** | **12** | **11** | **9** | **32** |

### 按问题类型统计

| 类型 | 数量 | 占比 |
|------|------|------|
| 代码错误 | 8 | 25% |
| 接口缺失 | 7 | 22% |
| 一致性问题 | 10 | 31% |
| 安全隐患 | 4 | 13% |
| 代码优化 | 3 | 9% |

---

## 🎖️ 优秀实践

### 后端优秀实践

1. ✅ **BaseController消除重复代码** - 有效减少Token解析重复
2. ✅ **双Token认证机制** - Access + Refresh Token平衡安全与体验
3. ✅ **环境隔离配置** - 开发/生产CORS配置分离
4. ✅ **统一响应格式** - Result<T>和PageResponse<T>封装
5. ✅ **限流注解** - @RateLimiter防止恶意请求

### 前端优秀实践

1. ✅ **Vue 3 Composition API** - 代码组织清晰，复用性强
2. ✅ **TypeScript类型安全** - 减少运行时错误
3. ✅ **组件化开发** - QuestionCard、AnswerCard等复用组件
4. ✅ **API模块化** - 按功能模块封装API
5. ✅ **Pinia状态管理** - 简洁的状态管理方案

---

## 📋 修复检查清单

### 立即修复（P0）

- [ ] 修复QuestionController.java:47格式问题
- [ ] 修复ResultCode.java错误码重复
- [ ] 修复SecurityConfig权限过宽问题
- [ ] 设置Druid监控密码
- [ ] 创建api/collection.ts文件
- [ ] 创建views/user/Settings.vue组件
- [ ] 添加GET /user/{userId}/stats接口
- [ ] 添加GET /tag/{tagId}/followed接口
- [ ] 添加GET /tag/{tagId}/question-count接口
- [ ] 统一搜索接口参数命名

### 近期修复（P1）

- [ ] 修复UserController空指针风险
- [ ] 将TagController关注逻辑移至Service
- [ ] 为管理员接口添加权限验证
- [ ] 统一类型定义（创建types/index.ts）
- [ ] 创建工具函数模块（utils/format.ts）
- [ ] 添加路由守卫
- [ ] 统一分页接口返回格式
- [ ] 修复批量检查接口参数格式

### 后续优化（P2）

- [ ] 提取分页composable
- [ ] 提取loading composable
- [ ] 提取公共样式
- [ ] 统一日志级别
- [ ] 补充单元测试

---

## 📝 总结

CampusZhihu项目整体代码质量良好，架构清晰，功能完整度较高。主要问题集中在：

1. **接口一致性**：前后端接口定义存在差异，需要统一
2. **分页支持**：部分列表接口缺少真实的分页支持
3. **缺失功能**：少数前端调用的接口后端未实现
4. **代码格式**：存在格式混乱和错误码重复问题

建议按照优先级逐步修复问题，确保项目在课程演示前达到最佳状态。

---

**审查人员**：代码审查agent
**审查时间**：2026-01-11
**下次审查建议**：修复完成后再次审查
